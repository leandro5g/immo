import styled from 'styled-components/native';

export const Container = styled.View`
  flex: 1;
  padding: 0px ${({ theme }) => theme.METRICS.MARGIN.x12}px;
`;

export const Content = styled.View`
  flex: 1;
`;